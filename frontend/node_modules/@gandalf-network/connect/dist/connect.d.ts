export type Services = {
    [key: string]: boolean;
};
export type ConnectInput = {
    publicKey: string;
    redirectURL: string;
    services: Services;
};
declare class Connect {
    publicKey: string;
    redirectURL: string;
    services: Services;
    verificationComplete: boolean;
    constructor(input: ConnectInput);
    generateURL(): Promise<string>;
    generateQRCode(): Promise<string>;
    static getSupportedServices(): Promise<String[]>;
    static getDataKeyFromURL(redirectURL: string): string;
    private allValidations;
    private static validatePublicKey;
    private static validateInputServices;
    private static validateRedirectURL;
}
export default Connect;
